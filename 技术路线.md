# 下班发车 · 技术路线 v1

> 原则：动效即产品，其他一切从简。
> 两条铁律：① 状态机与画面分离，联机能力可插拔；② 舞台演示不依赖会场网络。

---

## 1. 形态与技术栈

**形态**：手机尺寸的单页 Web App（设计基准 390×844）。
桌面/投影打开时居中显示一个手机壳框——评委投屏效果好，还显得完成度高。
不做原生 App、不做 PWA 推送（会内通知用 App 内横幅模拟）。

**技术栈（全部选最熟路径，不在选型上创新）**：

| 层 | 选择 | 理由 |
|---|---|---|
| 脚手架 | Vite + React + TypeScript | 起手 5 分钟，热更新快，动效调参全靠热更新 |
| 动效 | **Framer Motion（motion）** | 弹簧曲线、拖拽手势、layout 动画开箱即用——刷工牌拖拽、磁吸、slide-to-depart、票飞入箱全靠它，自己手写省不出这个时间 |
| 状态 | zustand（或 useReducer） | 一个 store 装下整个 TripState，够了 |
| 样式 | CSS 变量 + 普通 CSS（队里熟 Tailwind 就用 Tailwind） | 色板直接搬 pitch 页那套：发车绿 / 琥珀 / 金 |
| 彩带 | canvas-confetti（~2KB） | 别手写粒子系统，这钱不值得花 |
| 音效 | WebAudio 合成（嘀/咔嚓/鼓/汽笛都能合成） | 零音频资源文件；首次点击解锁 AudioContext |
| 震动 | `navigator.vibrate`（包一层 guard） | Android Chrome 有效；iOS 不支持，静默降级 |
| 部署 | Vercel / Netlify 静态托管 | 演示模式是纯静态，扫码即开 |

---

## 2. 核心架构：状态机与画面分离（唯一重要的架构决策）

整个产品 = 一个纯函数状态机 + 一层动效画面 + 一个可替换的驱动器。

```
┌─────────────┐  events   ┌──────────────┐  state   ┌─────────────┐
│   Driver     │ ────────▶ │  TripEngine  │ ───────▶ │  UI (React) │
│ ①LocalDemo   │           │  纯TS reducer │          │  分相位渲染  │
│ ②WebSocket   │ ◀──────── │  无副作用     │ ◀─────── │  动效编排    │
└─────────────┘  dispatch  └──────────────┘  actions └─────────────┘
```

- **TripEngine**：纯 TypeScript reducer，不碰 DOM、不碰网络。单元测试友好，两个驱动器共用。
- **LocalDemoDriver**（默认）：内置 5 个假人队友（定时错峰投票、自动接令），时间可被导演面板快进。**舞台演示用它，零网络依赖。**
- **WsDriver**（加分项）：同一套事件走 WebSocket，换驱动器不改任何 UI 代码。

### 数据模型（全部家当）

```ts
type Phase = 'idle' | 'boarding' | 'drawing' | 'departing'
           | 'departed' | 'settled' | 'suspended';

interface Member  { id: string; name: string; emoji: string }
interface Ticket  { memberId: string; seatNo: number; late?: boolean }

interface TripState {
  phase: Phase;
  departAt: number;        // 今日发车时刻
  members: Member[];
  tickets: Ticket[];
  conductors: string[];    // 列车长 memberId，开奖时定
  declined: string[];      // 今日已婉拒过的人（每人限一次）
  boarded: string[];       // 已上车
  streak: number;
  minCrew: number;         // 最少发车人数，默认 3
}
```

### 事件表（UI 与引擎的全部接口，也是未来服务端协议）

```
TICK                        时钟推进（驱动 idle→boarding→drawing 转相）
SUBMIT_TICKET(memberId)     刷工牌投票（drawing 后投 = 补票 late:true）
WITHDRAW_TICKET(memberId)   开奖前撤票
DRAW_RESULT(conductorIds)   开奖结果（谁抽的见下方"确定性"）
DECLINE_DUTY(memberId)      婉拒 → 引擎补抽一人
DEPART(memberId)            列车长滑动发车 → phase=departed
BOARD(memberId)             乘客确认上车
SETTLE / SUSPEND            结算 / 停运
```

**开奖确定性（联机同步的关键技巧）**：结果先定、动画后演。
Driver 先产出 `DRAW_RESULT`（本地随机或服务端广播），UI 拿着已知结果编排 20 秒悬念动画。
动画只是表演，不参与决定——多端同步瞬间变简单，摇号鼓永远不会"演错"。

**抽取规则**：`conductors = clamp(round(n/3), 1, 3)`；`n ≤ minCrew` 时全员列车长，跳过开奖动画直接发车相。

---

## 3. 里程碑（按砍需求顺序倒排，M0–M5 是必需品）

| 里程碑 | 内容 | 预估 | 验收标准 |
|---|---|---:|---|
| **M0 骨架** | 脚手架 + 色板 tokens + 手机壳框 + TripEngine 全事件 + LocalDemoDriver + 导演面板雏形 | 1.5h | 导演面板能快进相位，屏幕上有对应文字状态 |
| **M1 检票** | 工牌拖拽→磁吸→嘀→打印车票→飞入票箱→计数翻牌；假人错峰投票；撤票 | 3h | **第一爽点手感达标**：拖拽跟手、吸附干脆、回弹有弹性 |
| **M2 开奖** | 票箱变鼓→摇动渐强→逐张吐票→翻面→金章→confetti；音效+震动全套 | 3h | 20 秒编排一气呵成，第三张揭晓有全屏效果 |
| **M3 发车** | 发车令卡（接令/婉拒重抽）→ 收拾清单 → slide-to-depart → 车厢视图头像跳座 | 2.5h | 单机可切"列车长/乘客"双视角演完整流 |
| **M4 闭环** | IDLE 倒计时 + 结算页 streak+1（localStorage）+ 停运态 + App 内通知横幅 | 1.5h | 从平峰到收班全链路不断点，刷新不丢 streak |
| **M5 打磨** | 导演面板完善（一键跳任意相位、重置）、投影模式、部署上线 | 1.5h | 生成正式 URL + 二维码，评委手机扫码能玩 |

**必需品合计 ≈ 13 小时**（两人并行实际 7–8 小时墙钟时间）。

| 加分项 | 内容 | 预估 | 前提 |
|---|---|---:|---|
| S1 真联机 | ~150 行 Node + ws 内存房间服务器（Railway/Fly 部署）+ WsDriver + 扫码入队 | 3–4h | M0–M5 全部完成 |
| S2 吉祥物 | 按 deep-research-report 流程做班班 reaction | 3–5h | 有人闲着 |
| S3 建队流程 | 起名/定时间/邀请卡（当前硬编码 G604 / 18:00） | 2h | S1 之后才有意义 |

**S1 的演示价值**：让评委现场扫码变成车队成员，当着全场从评委手机里抽列车长——
如果 WiFi 争气，这是全场最炸的 demo；不争气，一键切回 LocalDemoDriver，观感零损失。
**默认舞台方案永远是演示模式，联机是彩蛋不是底座。**

### S1 联机设计：房间模型（派对游戏同款）

**组队方式**：建队 → 得车队码（G604）+ 邀请链接/二维码 → 同事点开填花名+emoji 即入房。
无账号：`memberId` 随机生成存各自 localStorage。

**单一裁判原则**：服务器跑同一份 `TripEngine`（前后端共用 reducer）。
手机只发意图、收快照、演结果。

**协议（全部消息类型）**：

| 方向 | 消息 | 说明 |
|---|---|---|
| 手机→服 | `JOIN {teamCode, memberId, name, emoji}` | 入房 / 重连，服务器回全量快照 |
| 手机→服 | `SUBMIT_TICKET` / `WITHDRAW_TICKET` | 刷工牌 / 撤票 |
| 手机→服 | `DECLINE_DUTY` / `DEPART` / `BOARD` | 婉拒 / 滑动发车 / 已上车 |
| 服→全房 | `STATE {state}` | **全量快照**（<2KB，不做增量同步） |
| 服→全房 | `DRAW_RESULT {conductors, drawStartAt}` | 见下 |

**"一起抽奖"= 结果先定 + 同步开演**：
1. T-0 由**服务器定时器**触发抽签（全宇宙只抽这一次，`Math.random()` 即可，熟人小队不需要防作弊）；
2. 广播 `DRAW_RESULT`，`drawStartAt` = 服务器时间 +2s；
3. 每部手机在 `drawStartAt` 时刻同时开播 20 秒摇号动画——结果已知、编排确定，
   所有人同一秒看到同一张票翻面。被抽中者本机判断 `conductors.includes(me)` 加播金光重震。

**两个必须处理的坑**：
1. **锁屏掉线**：iOS 锁屏即杀 WebSocket。监听 `visibilitychange` 回前台立即重连，
   服务器对重连 JOIN 无脑回快照。不做 = 演示当天头号杀手。
2. **时钟归属**：T-30 / T-0 全由服务器定时器驱动，手机倒计时仅显示；
   托管平台选常驻实例或加 keep-alive，防免费层休眠错过发车。

**服务器体量**：单文件 Node + ws，`Map<teamCode, room>` 内存房间，约 150 行，无数据库
（streak 想过夜就加一个 JSON 文件落盘，黑客松够用）。

---

## 4. 分工建议（两人版）

- **A：动效手**——M1→M2→M3 的画面编排，全职调 Framer Motion 弹簧参数和节奏
- **B：引擎手**——M0 引擎+驱动器+导演面板 → M4 闭环 → S1 服务器
- **你（产品）**——文案口径、demo 脚本（90 秒串场词）、每个爽点的手感验收（"不好玩就重做"是你的活）

M0 完成后 A、B 完全解耦：A 面向引擎发假事件调画面，B 不碰画面。

---

## 5. 已知的坑（提前踩掉）

1. **移动端拖拽与滚动打架**：工牌、滑块元素必须 `touch-action: none`，否则拖一半页面跟着滚。
2. **iOS 音频锁**：AudioContext 必须在首次用户点击里 resume（M1 就处理，别拖到最后）。
3. **iOS 无震动 API**：guard 后静默跳过，别报错。
4. **100vh 问题**：手机浏览器地址栏收缩会跳，用 `100dvh`。
5. **手感调参是无底洞**：每个爽点限时 timebox（M1 拖拽最多调 45 分钟），先过 80 分线推进，收尾再回来磨。
6. **confetti 性能**：低端机 particleCount ≤ 120，别开 3 处同时喷。
7. **演示当天**：提前把静态包存本地，`npx serve` 也能开——断网也能演是底线。

---

## 6. 起手命令（M0 现在就能开跑）

```bash
npm create vite@latest fache -- --template react-ts
cd fache
npm i motion zustand canvas-confetti
npm i -D @types/canvas-confetti
npm run dev
```

目录结构：

```
src/
  engine/            # 纯逻辑，无 DOM
    types.ts         # TripState / 事件类型
    reducer.ts       # TripEngine
    rules.ts         # 抽取规则 / 转相条件
  drivers/
    localDemo.ts     # 假人 + 可快进时钟
    ws.ts            # (S1)
  ui/
    App.tsx          # 手机壳框 + 相位路由
    phases/          # Idle / Boarding / Drawing / Departing / Settled / Suspended
    fx/              # confetti / sound / haptics 封装
    director/        # 导演面板（?director=1 时显示）
  theme.css          # 色板 tokens（搬 pitch 页）
```
